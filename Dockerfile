# TODO: Write your Dockerfile
#
# Requirements:
# - Base image: python:3.14-slim (pin version, no :latest)
# - Install dependencies from requirements.txt
# - Run as a non-root user
# - EXPOSE 8080
# - Start: uvicorn src.app:app --host 0.0.0.0 --port 8080
